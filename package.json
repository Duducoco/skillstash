{
  "name": "skill-sync",
  "version": "0.5.0",
  "description": "Personal skill management system with multi-device & multi-agent sync",
  "type": "module",
  "bin": {
    "skill-sync": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["skill", "sync", "agent", "workbuddy", "codex", "claude", "cli"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^13.1.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
